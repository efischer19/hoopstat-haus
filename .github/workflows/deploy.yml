---
name: Deploy Applications

on:
  workflow_dispatch:
    inputs:
      application:
        description: 'Application to deploy'
        required: true
        type: choice
        options:
          - silver-processing
          - gold-analytics
      environment:
        description: 'Target environment'
        required: true
        default: 'prod'
        type: choice
        options:
          - prod
      image_tag:
        description: 'Image tag to deploy (defaults to latest)'
        required: false
        default: 'latest'
        type: string
  workflow_run:
    workflows: ["CI"]
    types: [completed]

permissions:
  id-token: write
  contents: read

env:
  AWS_REGION: us-east-1
  ECR_REPOSITORY: hoopstat-haus/prod

jobs:
  detect-deployable-apps:
    # Only run automatically for workflow_run events that originated from a push to main
    if: >-
      github.event_name != 'workflow_run' ||
      (github.event_name == 'workflow_run' && github.event.workflow_run.event == 'push' && github.event.workflow_run.head_branch == 'main')
    runs-on: ubuntu-latest
    outputs:
      app-matrix: ${{ steps.dispatch-defaults.outputs.app-matrix || steps.changes.outputs.app-matrix }}
      has-deployable-changes: ${{ steps.dispatch-defaults.outputs.has-deployable-changes || steps.changes.outputs.has-deployable-changes }}
    steps:
      - name: Set defaults for workflow_dispatch
        if: github.event_name == 'workflow_dispatch'
        id: dispatch-defaults
        run: |
          echo "has-deployable-changes=true" >> $GITHUB_OUTPUT
          echo "app-matrix={\"include\":[{\"app\":\"${{ inputs.application }}\"}]}" >> $GITHUB_OUTPUT

      - name: Checkout code (push)
        if: github.event_name == 'push'
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Checkout code (workflow_run)
        if: github.event_name == 'workflow_run'
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.workflow_run.head_sha }}

      - name: Detect deployable applications
        if: github.event_name != 'workflow_dispatch'
        id: changes
        run: |
          set -eo pipefail
          echo "🔎 Determining changed apps for ${GITHUB_EVENT_NAME}..."

          if [ "${{ github.event_name }}" = "push" ]; then
            BASE_SHA="${{ github.event.before }}"
            HEAD_SHA="${{ github.sha }}"
          else
            # workflow_run: use head_sha from the CI run and first parent as base
            HEAD_SHA="${{ github.event.workflow_run.head_sha }}"
            # Ensure we have the commit and its parent
            git fetch --no-tags --depth=2 origin "+$HEAD_SHA:$HEAD_SHA" || true
            git checkout -q "$HEAD_SHA" || true
            BASE_SHA=$(git rev-parse "$HEAD_SHA^" 2>/dev/null || true)
          fi

          echo "BASE_SHA=$BASE_SHA"
          echo "HEAD_SHA=$HEAD_SHA"

          if [ -z "$BASE_SHA" ] || echo "$BASE_SHA" | grep -q '^0\{40\}$'; then
            echo "⚠️  No valid BASE_SHA; using previous commit as base"
            BASE_SHA=$(git rev-parse HEAD~1 || true)
          fi

          if [ -z "$BASE_SHA" ]; then
            echo "⚠️  No valid BASE_SHA found; treating as initial commit"
            changed_app_files=$(git ls-files 'apps/*' | cut -d'/' -f2 | sort -u)
          else
            echo "🔁 Diffing changes between $BASE_SHA..$HEAD_SHA"
            changed_app_files=$(git diff --name-only "$BASE_SHA" "$HEAD_SHA" | grep '^apps/' || true)
            changed_app_files=$(echo "$changed_app_files" | cut -d'/' -f2 | sort -u)
          fi

          if [ -z "$changed_app_files" ]; then
            echo "has-deployable-changes=false" >> $GITHUB_OUTPUT
            echo "app-matrix={\"include\":[]}" >> $GITHUB_OUTPUT
            echo "No changes detected in apps directory"
          else
            app_matrix_items=""
            found_deployable=false
            for app in $changed_app_files; do
              if [ -f "apps/$app/Dockerfile" ]; then
                if [ -n "$app_matrix_items" ]; then
                  app_matrix_items="$app_matrix_items,"
                fi
                app_matrix_items="$app_matrix_items{\"app\":\"$app\"}"
                found_deployable=true
                echo "📦 Found deployable app: $app"
              fi
            done

            if [ "$found_deployable" = "true" ]; then
              echo "has-deployable-changes=true" >> $GITHUB_OUTPUT
              echo "app-matrix={\"include\":[$app_matrix_items]}" >> $GITHUB_OUTPUT
              echo "Found deployable applications with changes"
            else
              echo "has-deployable-changes=false" >> $GITHUB_OUTPUT
              echo "app-matrix={\"include\":[]}" >> $GITHUB_OUTPUT
              echo "No deployable applications found in changed directories"
            fi
          fi

  deploy-application:
    name: Deploy ${{ matrix.app || inputs.application }}
    runs-on: ubuntu-latest
    needs: detect-deployable-apps
    if: ${{ (github.event_name == 'push' && needs.detect-deployable-apps.outputs.has-deployable-changes == 'true') || (github.event_name == 'workflow_dispatch' && needs.detect-deployable-apps.outputs.has-deployable-changes == 'true') || (github.event_name == 'workflow_run' && github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.event == 'push' && github.event.workflow_run.head_branch == 'main' && needs.detect-deployable-apps.outputs.has-deployable-changes == 'true') }}
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.detect-deployable-apps.outputs.app-matrix) }}

    steps:
      - name: Set deployment variables
        id: vars
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "app=${{ inputs.application }}" >> $GITHUB_OUTPUT
            echo "environment=${{ inputs.environment }}" >> $GITHUB_OUTPUT
            echo "image_tag=${{ inputs.image_tag }}" >> $GITHUB_OUTPUT
          elif [ "${{ github.event_name }}" = "workflow_run" ]; then
            echo "app=${{ matrix.app }}" >> $GITHUB_OUTPUT
            echo "environment=prod" >> $GITHUB_OUTPUT
            echo "image_tag=${{ github.event.workflow_run.head_sha }}" >> $GITHUB_OUTPUT
          else
            echo "app=${{ matrix.app }}" >> $GITHUB_OUTPUT
            echo "environment=prod" >> $GITHUB_OUTPUT
            echo "image_tag=${{ github.sha }}" >> $GITHUB_OUTPUT
          fi

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::${{ secrets.AWS_ACCOUNT_ID }}:role/hoopstat-haus-operations
          role-session-name: GitHubActions-Deploy-${{ github.run_id }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Install deployment dependencies
        run: |
          # Install jq for JSON processing
          sudo apt-get update && sudo apt-get install -y jq

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Verify image exists in ECR
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          APP_NAME: ${{ steps.vars.outputs.app }}
          IMAGE_TAG: ${{ steps.vars.outputs.image_tag }}
        run: |
          echo "🔍 Checking if image exists in ECR..."

          # Require exact SHA tag when triggered by push/workflow_run
          REQUIRE_EXACT=true
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            REQUIRE_EXACT=false
          fi

          if aws ecr describe-images \
              --repository-name $ECR_REPOSITORY \
              --image-ids imageTag=$APP_NAME-$IMAGE_TAG \
              --region $AWS_REGION >/dev/null 2>&1; then
            echo "✅ Image found: $ECR_REGISTRY/$ECR_REPOSITORY:$APP_NAME-$IMAGE_TAG"
            echo "FINAL_IMAGE_TAG=$APP_NAME-$IMAGE_TAG" >> $GITHUB_ENV
          else
            echo "❌ Specific image not found: $ECR_REGISTRY/$ECR_REPOSITORY:$APP_NAME-$IMAGE_TAG"
            if [ "$REQUIRE_EXACT" = true ]; then
              echo "💥 Failing fast because required image tag is missing. Ensure CI pushed $APP_NAME-$IMAGE_TAG before deploy."
              exit 1
            fi

            echo "🔍 Checking for latest image (manual run fallback)..."
            if aws ecr describe-images \
                --repository-name $ECR_REPOSITORY \
                --image-ids imageTag=$APP_NAME-latest \
                --region $AWS_REGION >/dev/null 2>&1; then
              echo "✅ Using latest image: $ECR_REGISTRY/$ECR_REPOSITORY:$APP_NAME-latest"
              echo "FINAL_IMAGE_TAG=$APP_NAME-latest" >> $GITHUB_ENV
            else
              echo "❌ No images found for $APP_NAME"
              echo "Available images for $APP_NAME:"
              aws ecr describe-images \
                --repository-name $ECR_REPOSITORY \
                --query "imageDetails[?imageTags!=null].imageTags[?starts_with(@, '$APP_NAME-')]" \
                --output text 2>/dev/null | tr '\t' '\n' | sort || echo "No images found"
              exit 1
            fi
          fi

      - name: Pull and test image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          APP_NAME: ${{ steps.vars.outputs.app }}
        run: |
          echo "📥 Pulling image from ECR..."
          docker pull $ECR_REGISTRY/$ECR_REPOSITORY:$FINAL_IMAGE_TAG

          echo "🧪 Testing image health..."
          
          # Check if this is a Lambda-based application by looking for the lambda entrypoint
          # Use --entrypoint to override the Lambda entrypoint for this check
          if docker run --rm --entrypoint sh $ECR_REGISTRY/$ECR_REPOSITORY:$FINAL_IMAGE_TAG \
              -c 'ls /lambda-entrypoint.sh 2>/dev/null || echo "not-lambda"' | grep -q "not-lambda"; then
            echo "🐍 Regular Python application detected"
            # Run a basic health check for regular Python applications
            if docker run --rm --name test-$APP_NAME \
              $ECR_REGISTRY/$ECR_REPOSITORY:$FINAL_IMAGE_TAG \
              python -c 'import app.main; print("Health check passed - app.main imported successfully")'; then
              echo "✅ Regular Python application health check passed"
            else
              echo "❌ Image health check failed"
              exit 1
            fi
          else
            echo "🚀 Lambda application detected"
            # For Lambda functions, test the handler import and basic functionality
            # Use --entrypoint to override the Lambda entrypoint for health checks
            if docker run --rm --name test-$APP_NAME \
              --entrypoint /var/lang/bin/python \
              $ECR_REGISTRY/$ECR_REPOSITORY:$FINAL_IMAGE_TAG \
              -c 'from app.lambda_handler import lambda_handler; print("Lambda handler health check passed - handler is importable and callable") if callable(lambda_handler) else exit(1)'; then
              echo "✅ Lambda application health check passed"
            else
              echo "❌ Lambda handler health check failed"
              exit 1
            fi
          fi

          echo "✅ Image health check passed"

      - name: Deploy to AWS Lambda
        env:
          APP_NAME: ${{ steps.vars.outputs.app }}
          ENVIRONMENT: ${{ steps.vars.outputs.environment }}
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
        run: |
          # bronze-ingestion runs locally only (not deployed to Lambda)
          # The Lambda function was removed in infrastructure - see issue #412
          if [ "$APP_NAME" = "bronze-ingestion" ]; then
            echo "ℹ️  Skipping Lambda deployment for $APP_NAME"
            echo "📝 bronze-ingestion runs locally via Docker (see apps/bronze-ingestion/setup-local.sh)"
            echo "✅ Image is available in ECR for local use: $ECR_REGISTRY/$ECR_REPOSITORY:$FINAL_IMAGE_TAG"
            exit 0
          fi

          echo "🚀 Deploying $APP_NAME to AWS Lambda in $ENVIRONMENT..."
          echo "📍 Image: $ECR_REGISTRY/$ECR_REPOSITORY:$FINAL_IMAGE_TAG"

          # Lambda function name follows the pattern: hoopstat-haus-{app-name}
          FUNCTION_NAME="hoopstat-haus-$APP_NAME"
          IMAGE_URI="$ECR_REGISTRY/$ECR_REPOSITORY:$FINAL_IMAGE_TAG"

          echo "🔍 Checking if Lambda function exists: $FUNCTION_NAME"

          # Check if Lambda function exists
          if aws lambda get-function \
              --function-name "$FUNCTION_NAME" \
              --region "$AWS_REGION" >/dev/null 2>&1; then

            echo "✅ Lambda function exists, proceeding with update"
            echo "🔄 Updating Lambda function: $FUNCTION_NAME"
            echo "📦 New image URI: $IMAGE_URI"

            # Update Lambda function code with new container image
            aws lambda update-function-code \
              --function-name "$FUNCTION_NAME" \
              --image-uri "$IMAGE_URI" \
              --region "$AWS_REGION" \
              --output json > lambda_update_result.json

            # Check if update was successful
            if [ $? -eq 0 ]; then
              echo "✅ Lambda function code updated successfully"

              # Extract some information from the response
              LAST_MODIFIED=$(cat lambda_update_result.json | jq -r '.LastModified')
              CODE_SHA256=$(cat lambda_update_result.json | jq -r '.CodeSha256')
              FUNCTION_ARN=$(cat lambda_update_result.json | jq -r '.FunctionArn')

              echo "📋 Update Details:"
              echo "  Function ARN: $FUNCTION_ARN"
              echo "  Last Modified: $LAST_MODIFIED"
              echo "  Code SHA256: $CODE_SHA256"

              # Wait for function to be active
              echo "⏳ Waiting for function to become active..."
              aws lambda wait function-updated \
                --function-name "$FUNCTION_NAME" \
                --region "$AWS_REGION"

              if [ $? -eq 0 ]; then
                echo "✅ Lambda function is now active and ready"
                echo "🎉 Deployment completed successfully"
              else
                echo "❌ Function update failed or timed out"
                exit 1
              fi
            else
              echo "❌ Failed to update Lambda function code"
              cat lambda_update_result.json || echo "No response file created"
              exit 1
            fi

          else
            echo "❌ Lambda function does not exist: $FUNCTION_NAME"
            echo ""
            echo "🏗️  Initial Lambda function deployment requires infrastructure setup:"
            echo "   1. Ensure Terraform infrastructure has been applied"
            echo "   2. Verify that the Lambda function is created with initial container image"
            echo "   3. Run this deployment workflow again"
            echo ""
            echo "📋 Expected Lambda function: arn:aws:lambda:$AWS_REGION:*:function:$FUNCTION_NAME"
            echo ""
            echo "🔧 To deploy infrastructure:"
            echo "   cd infrastructure/"
            echo "   terraform init"
            echo "   terraform plan"
            echo "   terraform apply"
            echo ""
            echo "📝 Note: Initial Lambda function creation requires placeholder images in ECR."
            echo "   The infrastructure creates functions, then this workflow updates them."
            exit 1
          fi

      - name: Deployment summary
        env:
          APP_NAME: ${{ steps.vars.outputs.app }}
          ENVIRONMENT: ${{ steps.vars.outputs.environment }}
          IMAGE_TAG: ${{ steps.vars.outputs.image_tag }}
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
        run: |
          echo "📋 Deployment Summary"
          echo "=================="
          echo "Application: $APP_NAME"
          echo "Environment: $ENVIRONMENT"
          echo "Requested Image Tag: $IMAGE_TAG"
          echo "Final Image Tag: $FINAL_IMAGE_TAG"
          echo "Final Image: $ECR_REGISTRY/$ECR_REPOSITORY:$FINAL_IMAGE_TAG"
          echo "Triggered by: ${{ github.event_name }}"
          
          if [ "$APP_NAME" = "bronze-ingestion" ]; then
            echo "Deployment Type: ECR Only (Local Docker execution)"
          else
            echo "Deployment Type: Lambda Function"
          fi
          
          echo "Status: ✅ Success"

      - name: Cleanup temporary files
        if: always()
        run: |
          # Clean up any temporary files created during deployment
          rm -f lambda_update_result.json test_response.json